apiVersion: v1
kind: Service
metadata:
  name: roboto-marketplace-dev
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: marketplace-dev
  ports:
  - name: http
#    nodePort: 32457
    port: 80
    protocol: TCP
    targetPort: 80